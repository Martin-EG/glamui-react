/**
 * Visual-effect values that don't fit any existing category (not a
 * color, size, or motion timing). Shared by LoadingOverlay and
 * CommandPalette's backdrop blur.
 */
export const effects = {
  blur: {
    sm: '2px',
  },
} as const;
